Working code against the original spec using flask.node and a mock_warehous_api
Didn"t have Flask installed so import flask was pulling from non existant resouses therefore I installed Flask.
I also did not have Requests installed therefore my import requests was flagged. Solution was to install Requests.
used pip install in both CMD and GitBash as administrators.
These tools are also new to me, I didn't realise how many programs are dependant on other programs to function for mock API and server capabilities.
my mock_warehouse__api.py code was (if __name__ == '_main_') instaed of (if __name__ == '__main__') the correct code which meant my code didn't run as intended untill i fixed it.